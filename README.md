# Student Course Management System

Console app for my Java summer training assignment. Manages students, courses,
and enrollments (OOP, Collections, Generics, custom Exceptions, Enums).

## Run

Java 17+, Maven. Run `Main.java`, or:
```bash
mvn exec:java -Dexec.mainClass="com.evan.Main"
```
